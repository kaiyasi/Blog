<div class="hero-scene" aria-hidden="true">
  <div class="scene-lines"><i></i><i></i><i></i><i></i><i></i></div>
  <div class="guitar-pair">
    <img class="guitar" src="/images/guitars/guitar-pair.png" alt="" width="1054" height="1492" decoding="async" fetchpriority="high" />
  </div>
</div>

<style>
  .hero-scene { position: absolute; inset: 0; overflow: hidden; pointer-events: none; }
  .scene-lines { position: absolute; right: -4vw; top: 18%; width: min(58vw, 760px); display: flex; flex-direction: column; gap: 8px; transform: rotate(-7deg); opacity: .46; }
  .scene-lines i { height: 1px; background: color-mix(in srgb, var(--border) 80%, transparent); }
  .scene-lines i:nth-child(3) { background: var(--accent); }
  .guitar-pair { position: absolute; z-index: 2; right: max(2rem, calc((100vw - 1120px) / 2)); bottom: 128px; width: min(38vw, 500px); height: min(66vh, 590px); opacity: .8; transform: translate3d(var(--guitar-x, 0), var(--guitar-y, 0), 0); transition: transform 180ms ease-out, opacity 220ms ease; }
  .guitar { width: 100%; height: 100%; object-fit: contain; filter: saturate(.88); }
  :global([data-theme='dark']) .guitar-pair { opacity: .74; }
  @media (max-width: 900px) {
    .scene-lines { width: 70vw; }
    .guitar-pair { right: 0; width: 43vw; }
  }
  @media (max-width: 700px) {
    .scene-lines { top: 58%; right: -25%; width: 95vw; }
    .guitar-pair { right: -.4rem; bottom: 158px; width: 58vw; height: 39%; opacity: .78; }
    .guitar { filter: saturate(.82); }
  }
  @media (prefers-reduced-motion: reduce) {
    .guitar-pair { transition: none; }
  }
</style>
